node_modules/
dist/
*.wasm
.DS_Store
*.log
/*.jpeg
test-results/
coverage/
vendor/
.opencode/
.claude/
browsergent-conversation-*.json
# Smoke harness run artifacts (contain real conversation data — never commit)
smoke/runs/
smoke/runs*.zip
smoke/runs*.tar.gz
# CRX signing key — pins the extension ID, never commit
*.pem
# Release packaging output
*.crx
release/
